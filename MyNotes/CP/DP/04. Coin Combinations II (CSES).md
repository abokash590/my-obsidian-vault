**Eita ==Iterative DP== diye korte hobe**

```cpp
void code(){
    int n,s;cin>>n>>s;
    vector<int>v(n);vin(v);
    vector<int>dp(s+1,0);
    dp[0]=1;
    for(int i=0;i<n;i++){
        for(int need=v[i];need<=s;need++){
            dp[need]+=dp[need-v[i]];
            if(dp[need]>=MOD)dp[need]-=MOD;
        }
    }cout<<dp[s]<<endl;
}
```